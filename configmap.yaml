apiVersion: v1
kind: ConfigMap
metadata:
  name: demo-config
data:
  app.properties: |
    environment=development
    debug=true
    log_level=info
  message.txt: |
    Welcome to the demo application!
